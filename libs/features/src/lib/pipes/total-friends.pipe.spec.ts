import { TotalFriendsPipe } from './total-friends.pipe';

describe('TotalFriendsPipe', () => {
  it('create an instance', () => {
    const pipe = new TotalFriendsPipe();
    expect(pipe).toBeTruthy();
  });
});
